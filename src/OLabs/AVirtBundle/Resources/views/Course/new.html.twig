{% extends '::base.html.twig' %}

{% block body -%}
    <h3 id="myModalLabel">Crear evento o curso<button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button></h3>

    <form action="{{ path('course_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('course') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
